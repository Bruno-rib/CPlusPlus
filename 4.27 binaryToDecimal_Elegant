#include <cmath>
#include <iostream>
#include <stdio.h>
using namespace std;

int d = 0;
int dn = 0;
int inp = 111;

int main()
{
    for (int i = 0; inp > 0; i++) {
        d = inp % 10;
        inp = inp / 10;
        dn = dn + d * pow (2, i);
    }
    cout << dn;
}

